[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tcomm"
version = "0.1.0"
description = "Time conversion command-line tool"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
tcomm = "tcomm:main"
